import { normalizeEmail } from './utils.js';

export const id = 'q-tds-project-telegram-bot';
export const title = 'Q5: Data Analyst Telegram Bot';

const USERNAME_RE = /^[a-zA-Z][a-zA-Z0-9_]{3,30}[a-zA-Z0-9]$/;

export async function solve(email) {
  const norm = normalizeEmail(email);

  const summary = [
    `Build a Telegram bot that, when messaged a data-analysis question, works out the answer`,
    `and replies with exactly one JSON object: {"answer": ..., "log_url": "https://.../run.jsonl"}.`,
    `Keep the code in a public GitHub repo and keep the bot online for grading. This can't be`,
    `auto-built for you — it's a real bot you deploy — but the guide below walks through every`,
    `step a complete beginner needs: making the bot, writing the brain, hosting it 24/7, logging`,
    `runs, and registering your GitHub URL + Telegram username in the exam.`
  ].join(' ');

  const guide = [
    `## Q5 — Data Analyst Telegram Bot: step-by-step for a total beginner (${norm})`,
    ``,
    `### 🧒 First, in plain English: what are we building?`,
    `We are building a **robot that lives inside Telegram**. You can message it a question like`,
    `*"Which state has the highest maternal mortality rate?"* and it will think about the answer`,
    `using an AI model, then text you back a very specific, machine-readable reply — just one`,
    `JSON object, nothing else. A teacher's grading script will message your bot with real`,
    `questions and check whether the reply is correct and correctly shaped.`,
    ``,
    `You do **not** need to know the answer to every possible question in advance. Your bot's`,
    `"brain" is an AI model (an LLM) that figures out the answer live, the same way ChatGPT does`,
    `when you ask it something.`,
    ``,
    `### 🧩 The 6 pieces you're building (do them in this order)`,
    `1. **A Telegram bot** — a few clicks with an app called BotFather.`,
    `2. **Bot code** — a small Python program that listens for messages.`,
    `3. **A "brain"** — code inside your program that sends the question to an LLM (via aipipe.org)`,
    `   and gets an answer back.`,
    `4. **A run log** — every question + answer your bot handles gets written to a file`,
    `   (\`run.jsonl\`) that anyone can download.`,
    `5. **Hosting** — your code needs to run somewhere all the time, not just on your laptop`,
    `   while it's open.`,
    `6. **Registration** — telling the exam your GitHub repo link and your bot's username.`,
    ``,
    `---`,
    ``,
    `### Step 1 — Create your Telegram bot (5 minutes, no coding)`,
    `1. Open Telegram (the app or [web.telegram.org](https://web.telegram.org)) and search for`,
    `   **@BotFather** — this is Telegram's own official "bot that makes bots".`,
    `2. Start a chat with it and send the command \`/newbot\`.`,
    `3. It asks for a **display name** (anything, e.g. "My Data Analyst") — this can have spaces.`,
    `4. It asks for a **username** — this is stricter. It must:`,
    `   - be 5–32 characters`,
    `   - contain only letters, digits, and underscores`,
    `   - **end in the word "bot"** (e.g. \`my_data_bot\`, \`gauravdatabot\`)`,
    `5. BotFather replies with a message containing your **bot token** — a long string like`,
    `   \`123456789:AAExampleTokenTextHere\`. **Copy this and keep it secret** — anyone with this`,
    `   token can control your bot. Never commit it into your public GitHub repo (see Step 6).`,
    ``,
    `### Step 2 — Get your AI ("brain") access token`,
    `1. Go to [aipipe.org/login](https://aipipe.org/login) and sign in with your **student email**`,
    `   (${norm}).`,
    `2. Copy the token shown — it's a long string starting with \`eyJ...\`. This lets your code`,
    `   ask an AI model questions, the same way Q3/Q4's agents did.`,
    ``,
    `### Step 3 — Write the bot's code (beginner-friendly Python)`,
    `You need Python installed ([python.org](https://python.org) if you don't have it). Create a`,
    `new folder for your project and inside it a file called \`bot.py\`.`,
    ``,
    `Install the two libraries you need:`,
    '```bash',
    `pip install python-telegram-bot openai`,
    '```',
    ``,
    `Now paste this starter code into \`bot.py\` — it already does everything the question asks`,
    `for (receive a message, ask the AI, reply with the exact JSON shape, and log the run):`,
    '```python',
    `import json`,
    `import time`,
    `import os`,
    `from openai import OpenAI`,
    `from telegram import Update`,
    `from telegram.ext import ApplicationBuilder, MessageHandler, ContextTypes, filters`,
    ``,
    `# --- fill these in with your own values ---`,
    `TELEGRAM_BOT_TOKEN = "PASTE_YOUR_BOTFATHER_TOKEN_HERE"`,
    `AIPIPE_TOKEN = "PASTE_YOUR_AIPIPE_TOKEN_HERE"`,
    `LOG_URL = "PASTE_YOUR_PUBLIC_LOG_URL_HERE"  # see Step 5 — where run.jsonl will be hosted`,
    `# -------------------------------------------`,
    ``,
    `client = OpenAI(base_url="https://aipipe.org/openai/v1", api_key=AIPIPE_TOKEN)`,
    `LOG_FILE = "run.jsonl"`,
    ``,
    `# Keeps the last few messages per chat, so multi-turn questions work —`,
    `# "answer the LAST message" still needs the earlier ones for context.`,
    `conversation_history = {}`,
    ``,
    `def log_event(event: dict):`,
    `    event["timestamp"] = time.time()`,
    `    with open(LOG_FILE, "a") as f:`,
    `        f.write(json.dumps(event) + "\\n")`,
    ``,
    `async def handle_message(update: Update, context: ContextTypes.DEFAULT_TYPE):`,
    `    chat_id = update.effective_chat.id`,
    `    user_text = update.message.text`,
    `    log_event({"type": "incoming", "chat_id": chat_id, "text": user_text})`,
    ``,
    `    history = conversation_history.setdefault(chat_id, [])`,
    `    history.append({"role": "user", "content": user_text})`,
    ``,
    `    # Ask the AI to work out the answer. The system prompt tells it exactly how to`,
    `    # format the final reply — this is the part that MUST match what the question asked.`,
    `    system_prompt = (`,
    `        "You are a careful data analyst. The user's LAST message asks a data-analysis "`,
    `        "question and tells you exactly what JSON shape to reply with. Work out the "`,
    `        "real answer (use any public data you know, e.g. MOSPI statistics, general "`,
    `        "world knowledge, or arithmetic on numbers given in the message). "`,
    `        "Reply with ONLY that exact JSON object and absolutely nothing else — no "`,
    `        "explanation, no markdown, no code fences, just the raw JSON."`,
    `    )`,
    `    response = client.chat.completions.create(`,
    `        model="gpt-5-mini",`,
    `        messages=[{"role": "system", "content": system_prompt}] + history[-6:],`,
    `    )`,
    `    reply_text = response.choices[0].message.content.strip()`,
    `    history.append({"role": "assistant", "content": reply_text})`,
    ``,
    `    # Make sure we actually reply with valid JSON containing "log_url" — if the model`,
    `    # forgot the log_url field or wrapped it in markdown, fix it up here so the grader`,
    `    # never sees a malformed reply.`,
    `    try:`,
    `        parsed = json.loads(reply_text)`,
    `    except json.JSONDecodeError:`,
    `        # Model added extra text — try to pull out just the {...} part.`,
    `        start, end = reply_text.find("{"), reply_text.rfind("}")`,
    `        parsed = json.loads(reply_text[start:end + 1])`,
    `    parsed["log_url"] = LOG_URL`,
    `    final_reply = json.dumps(parsed)`,
    ``,
    `    log_event({"type": "outgoing", "chat_id": chat_id, "text": final_reply})`,
    `    await update.message.reply_text(final_reply)`,
    ``,
    `app = ApplicationBuilder().token(TELEGRAM_BOT_TOKEN).build()`,
    `app.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_message))`,
    `print("Bot is running... (Ctrl+C to stop)")`,
    `app.run_polling()`,
    '```',
    ``,
    `Run it locally to test first:`,
    '```bash',
    `python bot.py`,
    '```',
    `Now open Telegram, find your bot by its username, and send it a test question, e.g.`,
    `*"What is 15% of 200? Reply with ONLY this JSON: {\\"answer\\": <number>, \\"log_url\\": \\"...\\"}"*.`,
    `You should get back exactly one JSON line as a reply.`,
    ``,
    `### Step 4 — Test against the real grading pipeline before you deploy anything`,
    `The exact test harness used for grading is public. Clone it and point it at your bot:`,
    '```bash',
    `git clone https://github.com/Jivraj-18/tds-p1-t2-2026-telegram-bot`,
    '```',
    `Open its \`evals/questions.json\` and add a few of your own sample data-analysis questions`,
    `(these are just for your own practice — the real graded questions are different and kept`,
    `private). Run its test script against your bot's username to see if your replies pass its`,
    `shape checks before you spend time deploying.`,
    ``,
    `### Step 5 — Host your run log somewhere public (so \`log_url\` actually works)`,
    `\`log_url\` must be a link anyone (including the grader's computer) can download with`,
    `\`wget\` — no login required. The simplest beginner-friendly option, since you already have a`,
    `GitHub repo:`,
    `1. Put \`run.jsonl\` inside your GitHub repo folder and commit/push it periodically (even a`,
    `   simple line in your code that does \`os.system("git add run.jsonl && git commit -m log && `,
    `   git push")\` every few messages works, or just push manually every so often).`,
    `2. Your public log URL is then the **raw** GitHub link, which looks like:`,
    `   \`https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/run.jsonl\``,
    `3. Test it: paste that URL into a new browser tab (or run \`wget <url>\`) — it should show`,
    `   the raw file contents, not a GitHub webpage. If you see a webpage instead of raw text,`,
    `   you copied the normal GitHub link instead of the "Raw" one — click the **"Raw"** button`,
    `   on the file's GitHub page to get the correct URL.`,
    `- **Alternative** (if you'd rather not commit logs to git constantly): reuse a public GCS`,
    `  bucket the same way you did in Q3/Q4 — upload \`run.jsonl\` there and make it public, then`,
    `  use its \`https://storage.googleapis.com/BUCKET/run.jsonl\` URL instead.`,
    ``,
    `### Step 6 — Push your code to a public GitHub repository`,
    `1. Go to [github.com/new](https://github.com/new), create a repository, and make sure it's`,
    `   set to **Public** (private repos fail grading — the checker can't see them).`,
    `2. **Before pushing, remove your secret tokens from the code** — replace the hardcoded`,
    `   \`TELEGRAM_BOT_TOKEN\` and \`AIPIPE_TOKEN\` values with environment variables instead, e.g.:`,
    '```python',
    `TELEGRAM_BOT_TOKEN = os.environ["TELEGRAM_BOT_TOKEN"]`,
    `AIPIPE_TOKEN = os.environ["AIPIPE_TOKEN"]`,
    '```',
    `   and set those two environment variables wherever you host the bot (Step 7), never in the`,
    `   committed code itself. Committing a real token to a public repo lets anyone hijack your`,
    `   bot or spend your AI credits.`,
    `3. Push your code:`,
    '```bash',
    `git init`,
    `git add bot.py requirements.txt`,
    `git commit -m "Initial data analyst bot"`,
    `git branch -M main`,
    `git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git`,
    `git push -u origin main`,
    '```',
    `   (Create a \`requirements.txt\` with \`python-telegram-bot\` and \`openai\` listed, one per`,
    `   line, so anyone — including the grader, if they check your code — can install the same`,
    `   dependencies.)`,
    ``,
    `### Step 7 — Keep it running all the time (hosting)`,
    `Your laptop being asleep = your bot being offline = zero marks when the grader messages it.`,
    `You need a small always-on server. Beginner-friendly free-tier options:`,
    `- **Render.com** (Background Worker, not Web Service, since this bot doesn't listen on a`,
    `  port) — connect your GitHub repo, set the start command to \`python bot.py\`, add your two`,
    `  environment variables in its dashboard, and deploy. Free background workers may sleep`,
    `  after inactivity on some plans — check Render's current free-tier limits before relying`,
    `  on it for grading day.`,
    `- **Railway.app** — similar flow: connect repo, add environment variables, deploy as a`,
    `  worker process.`,
    `- **A cheap always-on VPS** (if you have one) — just \`nohup python bot.py &\` or run it`,
    `  inside a \`tmux\`/\`screen\` session, or better, set it up as a \`systemd\` service so it`,
    `  restarts automatically if the machine reboots.`,
    ``,
    `Whichever you pick, message your bot from your phone **after deploying** (not just when`,
    `testing locally) to confirm it replies from the live server, not your laptop.`,
    ``,
    `### Step 8 — Register your bot in the exam`,
    `Paste **exactly two comma-separated values** into the answer box for this question:`,
    '```text',
    `https://github.com/YOUR_USERNAME/YOUR_REPO, your_bot_username`,
    '```',
    `The exam's own validator checks:`,
    `- The first value is a real, reachable GitHub repo URL in the form`,
    `  \`https://github.com/owner/repo\` (checked live against the GitHub API — a 404 or private`,
    `  repo fails immediately).`,
    `- The second value, after stripping a leading \`@\` or a \`https://t.me/\` prefix, matches`,
    `  Telegram's username rules (5–32 chars, letters/digits/underscore) **and ends in "bot"**.`,
    ``,
    `Passing this registration step auto-awards a small confirmation mark (0.1) immediately —`,
    `**the remaining marks are graded after the deadline** by actually messaging your live bot`,
    `with real questions and checking the \`answer\` field and reviewing your \`log_url\`.`,
    ``,
    `### 🚨 Common mistakes that cost marks`,
    `- **Bot offline at grading time** — the #1 failure. Test from your phone after deploying,`,
    `  not just on your laptop.`,
    `- **Extra text around the JSON** — replying with \`Sure! Here's the answer: {...}\` instead`,
    `  of just \`{...}\` fails the "exactly one JSON object" requirement.`,
    `- **Wrong JSON shape** — always match the *exact* keys the incoming message asks for (it`,
    `  varies per question — read it every time, don't assume a fixed shape).`,
    `- **Multi-turn questions answered from the wrong message** — always answer the **last**`,
    `  message in a conversation, using earlier messages only as context.`,
    `- **\`log_url\` not actually public** — test it yourself in an incognito/private browser`,
    `  window (or with \`wget\`) with no login, to confirm a stranger's computer can fetch it.`,
    `- **Private GitHub repo** — the registration check calls the GitHub API and fails on 404s.`,
    `- **Telegram username doesn't end in "bot"**, or has invalid characters (only letters,`,
    `  digits, and underscore are allowed).`,
    `- **Real secrets committed to the public repo** — always use environment variables for`,
    `  \`TELEGRAM_BOT_TOKEN\` and \`AIPIPE_TOKEN\`, never hardcode them in code you push.`
  ].join('\n');

  return {
    type: 'guide',
    answer: summary,
    variant: `Telegram data-analyst bot walkthrough for ${norm}`,
    answerDisplay: [
      `### Q5: Data Analyst Telegram Bot`,
      ``,
      `This is the single biggest question in P1 (37.5 marks) and can't be auto-built for you —`,
      `it's a real bot you create, deploy, and keep online. This offline solver instead gives you`,
      `a complete, beginner-friendly build guide below.`,
      ``,
      summary,
      ``,
      `See the full step-by-step guide below: creating the bot with BotFather, writing the`,
      `Python code (full working starter script included), testing it, logging runs, hosting it`,
      `24/7, pushing to GitHub safely (without leaking your tokens), and registering your GitHub`,
      `repo + bot username in the exam.`
    ].join('\n'),
    guide
  };
}
